numpy>=1.24
torch>=2.0          # reward network + PPO training (next milestone)
gymnasium>=0.29     # RL environment interface (next milestone)
stable-baselines3>=2.0  # PPO implementation (next milestone)
